Consigne l'activité du jour dans `.solo/activite/<AAAA-MM-JJ>.md` (un fichier par
jour ; remplace celui d'aujourd'hui s'il existe). Base-toi sur le travail **réel**
(git log, diff, contexte), au format de `.claude/design/solo-contract.md` :

- `::: titre` — le thème du jour
- `::: analyse` — ce qui a été fait et **pourquoi**
- `::: sprint` — `{nom, statut, capacite, engages, completes, progression, joursRestants}`
- `::: kanban` — colonnes « À faire » / « En cours » / « Fait » avec leurs cartes
- `::: gantt` — (optionnel) tâches datées `{nom, debut, fin, couleur}`

Note libre / focus : $ARGUMENTS
