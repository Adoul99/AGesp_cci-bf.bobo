# AGesP — projet géré par Solo

- **Type** : laravel
- **Chemin** : C:\laragon\www\AGesP



## Connaissance du projet

La connaissance structurée vit dans `.solo/` (`fiche.md`, et les dossiers `git/`, `db/`,
`script/`, `architecture/`, `stack/`, `activite/`, `assets/`). Maintiens ces fichiers à
jour : Solo les ingère pour piloter le projet.

**Efficacité** : l'ingestion est **par fichier** (ré-écrire un fichier le remplace).
Quand tu changes le projet, mets à jour **uniquement le(s) fichier(s) `.solo/`
concerné(s)** (cf. la procédure §4), jamais tout le dossier.

Format exact (où + comment écrire) : **`.claude/design/solo-contract.md`**.
Conventions : `.claude/rules/solo.md`.
